#include <stdio.h>

int main() {
    float r = 5.5, area, circumference;
    area = 3.14*r*r;
    circumference = 2 * 3.14*r;
    printf("area is %f\n", area);
    printf("circumference is %f\n", circumference);
    return 0;
}